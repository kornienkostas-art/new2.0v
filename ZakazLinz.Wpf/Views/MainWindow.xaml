<Window x:Class="ZakazLinz.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:ZakazLinz.Wpf.Views"
        mc:Ignorable="d"
        Title="УссурОЧки.рф — Заказы линз" Height="720" Width="1200"
        WindowStartupLocation="CenterScreen"
        md:ThemeAssist.Theme="Dark">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="12">
            <TextBlock Text="УссурОЧки.рф" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Заказы МКЛ" Margin="8,0" Command="{Binding OpenMklOrdersCommand}"/>
                <Button Content="Заказы «Меридиан»" Margin="8,0" Command="{Binding OpenMeridianOrdersCommand}"/>
                <Button Content="Настройки" Margin="8,0" Command="{Binding OpenSettingsCommand}"/>
            </StackPanel>
        </DockPanel>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>